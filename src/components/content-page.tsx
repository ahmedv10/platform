import Link from "next/link";
import { PageHero } from "@/components/page-hero";

export function ContentPage({ eyebrow, title, accent, description, meta, introTitle, intro, cards, cta = "Talk to an expert" }: { eyebrow: string; title: string; accent: string; description: string; meta: string; introTitle: string; intro: string; cards: string[]; cta?: string }) { return <main><PageHero eyebrow={eyebrow} title={title} accent={accent} description={description} meta={meta} /><section className="section content-intro"><div className="two-column"><h2>{introTitle}</h2><p>{intro}</p></div></section><section className="section content-cards"><div className="section-label">01 / CAPABILITIES</div><div className="detail-grid">{cards.map((card, index) => <article className="detail-card" key={card}><span>{String(index + 1).padStart(2, "0")}</span><h3>{card}</h3><p>Practical capability aligned to customer context, operating reality, risk, and business outcomes.</p></article>)}</div></section><section className="page-cta"><h2>Make the next<br /><em>move.</em></h2><Link className="button button-primary" href="/contact">{cta} <b>↗</b></Link></section></main>; }
